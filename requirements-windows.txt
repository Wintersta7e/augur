# Windows-host-only dependencies for sensus/activity_monitor.py.
# DO NOT add these to requirements.txt — they fail to install on Linux.
#
# Install on Windows (where you run the activity_monitor daemon):
#   pip install -r requirements-windows.txt
#
# The daemon publishes PerceptionEvents to WSL's NATS over localhost,
# and reads augur:session:current from WSL's Redis over localhost
# (WSL2 forwards 127.0.0.1-bound ports to Windows automatically).

nats-py>=2.15.0
redis>=8.0.0
pywin32>=311
psutil>=7.2.2
pynput>=1.8.2
